<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AircloudTap" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/color_333333</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/color_333333</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">#000</item>

        <item name="colorPrimaryDark">@color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="colorControlNormal">@color/colorTextInput</item>
        <item name="android:actionBarSize">50dp</item>
    </style>
</resources>